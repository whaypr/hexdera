{
  "name": "elm-hex-grid",
  "private": true,
  "scripts": {
    "dev": "lamdera live",
    "build": "lamdera make src/Types.elm src/Backend.elm src/Frontend.elm",
    "format": "run-s format:elm format:prettier",
    "format:elm": "elm-format --yes ./src/",
    "format:prettier": "prettier --write README.md"
  },
  "license": "MIT",
  "devDependencies": {
    "elm-format": "^0.8.8",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.8.2"
  },
  "dependencies": {
    "lamdera": "^0.19.1-1.4.0"
  }
}
